variables:
  AUTO_DEVOPS_PRODUCTION_AUTO: "/^v/"

Test gitlab rule inline:
  extends: .autodevops_trigger_production
  rules:
    - if: "$PRODUCTION"
      when: never
    - if: /^test/ =~ $CI_COMMIT_REF_NAME
      when: on_success
    - if: "$CI_COMMIT_TAG"
      when: manual
